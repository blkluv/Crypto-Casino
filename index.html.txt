<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crypto Casino</title>
    <style>
        body {
            background-color: #111;
            color: #fff;
            font-family: Arial, sans-serif;
            text-align: center;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background: #222;
            border-radius: 10px;
            box-shadow: 0 0 10px #0f0;
        }
        button {
            background: #0f0;
            color: #000;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
            margin: 5px;
            font-size: 16px;
        }
        .leaderboard {
            margin-top: 20px;
            background: #333;
            padding: 10px;
            border-radius: 10px;
        }
    </style>
</head>
<body>

    <h1>Crypto Casino 🎰</h1>
    <p>Bet your **fake Solana** and test your luck!</p>
    <p><strong>Memecoin Contract Address:</strong> <span id="ca">[Insert Your CA]</span></p>

    <div class="container">
        <h2>Roulette 🎡</h2>
        <button onclick="playRoulette()">Spin</button>
        <p id="rouletteResult">Place your bet!</p>
    </div>

    <div class="container">
        <h2>Blackjack 🃏</h2>
        <button onclick="playBlackjack()">Draw</button>
        <p id="blackjackResult">Try to hit 21!</p>
    </div>

    <div class="leaderboard container">
        <h2>Leaderboard 🏆</h2>
        <ul id="leaderboard"></ul>
    </div>

    <script>
        let fakeSolana = 1000;
        let leaderboard = JSON.parse(localStorage.getItem("leaderboard")) || [];

        function updateLeaderboard() {
            let list = document.getElementById("leaderboard");
            list.innerHTML = leaderboard.map((entry, i) => `<li>${i + 1}. ${entry.name}: ${entry.score} SOL</li>`).join("");
        }

        function playRoulette() {
            let bet = Math.floor(Math.random() * 100);
            let win = Math.random() < 0.5;
            fakeSolana += win ? bet : -bet;
            document.getElementById("rouletteResult").textContent = win ? `You won ${bet} SOL!` : `You lost ${bet} SOL!`;
            saveScore();
        }

        function playBlackjack() {
            let score = Math.floor(Math.random() * 21) + 1;
            let win = score === 21;
            fakeSolana += win ? 200 : -50;
            document.getElementById("blackjackResult").textContent = win ? "BLACKJACK! +200 SOL" : `You got ${score}. -50 SOL`;
            saveScore();
        }

        function saveScore() {
            leaderboard.push({ name: "Player", score: fakeSolana });
            leaderboard = leaderboard.sort((a, b) => b.score - a.score).slice(0, 5);
            localStorage.setItem("leaderboard", JSON.stringify(leaderboard));
            updateLeaderboard();
        }

        updateLeaderboard();
    </script>

</body>
</html>